# -- General --
set -g default-terminal "tmux-256color"
set -ag terminal-overrides ",xterm-256color:RGB"
set -g mouse on
set -g base-index 1
setw -g pane-base-index 1
set -g renumber-windows on
set -g history-limit 50000
set -g escape-time 0
set -g focus-events on

# -- Prefix --
set -g prefix C-a
unbind C-b
bind C-a send-prefix

# -- Splits (use current path) --
bind | split-window -h -c "#{pane_current_path}"
bind - split-window -v -c "#{pane_current_path}"
unbind '"'
unbind %

# -- New window keeps current path --
bind c new-window -c "#{pane_current_path}"

# -- Pane navigation --
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# -- Pane resizing --
bind -r H resize-pane -L 5
bind -r J resize-pane -D 5
bind -r K resize-pane -U 5
bind -r L resize-pane -R 5

# -- Window navigation (prefix-less, matches local for BTT gestures via kitty) --
bind -n M-Left  previous-window
bind -n M-Right next-window

# -- Jump to last (highest-indexed) window with prefix + 0 --
bind 0 select-window -t :$

# -- Search (matches local: prefix + / from Kitty's Cmd+F) --
bind / copy-mode \; send-keys /

# -- Reload config --
bind r source-file ~/.tmux.conf \; display "Config reloaded"

# -- Plugins --
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'

# -- Plugin settings --
set -g @continuum-restore 'on'
set -g @continuum-save-interval '15'
set -g @resurrect-processes 'vi vim nvim emacs man less more tail top htop irssi mutt ssh watch'

# Initialize TPM (keep at bottom)
run '~/.tmux/plugins/tpm/tpm'
